// Replaced by wallet.ts — dollar-based wallet system (Phase 1 migration).
// Kept as empty module so any stale imports fail loudly at compile time
// rather than silently at runtime. Remove once API server is updated in Phase 2.
export {};
